Anacoda library

pip is a tool that we use to install package libraries that don't come with python natively.

Available web packages
Django, flask, pyramid, web2py, etc

Django Steps
1. Install it using pip
2. Identiify a folder where our projects will be put
3. create a django project with a command
4. create a django application with a command
5. Install the created app