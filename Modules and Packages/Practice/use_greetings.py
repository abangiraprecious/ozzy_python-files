# Create a file called greetings.py with a function say_hello(name) that returns "Hello, {name}!". Then create a second file that imports and uses it.

import greetings

print(greetings.say_hello("Precious"))

